{
  "buildCommand": "cd packages/db && POSTGRES_URL=$POSTGRES_URL npx drizzle-kit migrate && cd ../.. && turbo run build --filter=@kan/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
